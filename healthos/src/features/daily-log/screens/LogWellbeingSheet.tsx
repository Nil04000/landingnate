import { SheetScaffold } from '@/core/design-system/components';

/** Placeholder — implementación real en curso (Fase 2). */
export default function LogWellbeingSheet() {
  return <SheetScaffold title="Próximamente">{null}</SheetScaffold>;
}
