import { Screen } from '@/core/design-system/components';

/** Placeholder — implementación real en curso (Fase 3). */
export default function FoodCreateScreen() {
  return <Screen scroll={false}>{null}</Screen>;
}
