import { Screen } from '@/core/design-system/components';

/** Placeholder — implementación real en curso (Fase 5). */
export default function MarkerDetailScreen() {
  return <Screen scroll={false}>{null}</Screen>;
}
