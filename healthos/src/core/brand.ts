/**
 * Identidad de marca centralizada.
 *
 * "HealthOS" es un nombre de trabajo. Para renombrar la app:
 * 1. Cambiar `name` acá.
 * 2. Cambiar `name` y `slug` en `app.json`.
 * Nada más referencia el nombre directamente.
 */
export const brand = {
  name: 'HealthOS',
  tagline: 'Todo lo que afecta tu salud, registrado, analizado y transformado en decisiones.',
} as const;
